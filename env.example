# ========================================
# ENVIRONMENT VARIABLES CONFIGURATION
# ========================================

# DEVELOPMENT SETTINGS (for local development)
# Copy this file to .env and uncomment the values below

# Server Configuration
PORT=5001
NODE_ENV=development

# MongoDB Configuration (Local MongoDB)
MONGODB_URI=mongodb://localhost:27017/live-polling

# Frontend Environment Variables (for local development)
# REACT_APP_API_URL=http://localhost:5001
# REACT_APP_SOCKET_URL=http://localhost:5001

# ========================================
# PRODUCTION SETTINGS (for Vercel deployment)
# ========================================
# Set these in Vercel Dashboard → Settings → Environment Variables

# NODE_ENV=production
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/live-polling?retryWrites=true&w=majority

# Note: In production, REACT_APP_API_URL and REACT_APP_SOCKET_URL are not needed
# because the app uses relative URLs automatically 